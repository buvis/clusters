---

all:
  hosts:
  children:
    cluster:
      control_plane:
      worker:

    control_plane:
      children:
        chief:
        manager:

    chief:
      hosts:
        columbus:

    manager:
      hosts:
        nimitz:

    worker:
      hosts:
        feynman:
        planck:
        hawking:
        braun:

    standalone:
      db:
      player:

    db:
      hosts:
        domum

    player:
      hosts:
        hall:
        higgs:
