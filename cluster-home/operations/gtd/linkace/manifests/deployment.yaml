---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: linkace
  namespace: gtd
  labels:
    app.kubernetes.io/name: linkace
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: linkace
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: linkace
    spec:
      volumes:
        - name: la-cache
          emptyDir: {}
        - name: env-file
          emptyDir: {}
      initContainers:
        - name: write-env-as-app
          image: linkace/linkace:latest
          workingDir: /app
          command: ["sh", "-lc"]
          args:
            - >
              umask 027 &&
              mkdir -p /work &&
              {
                printf 'APP_KEY=%s\n' "$APP_KEY";
                printf 'DB_CONNECTION=%s\n' "${DB_CONNECTION:-pgsql}";
                printf 'DB_HOST=%s\n' "$DB_HOST";
                printf 'DB_PORT=%s\n' "${DB_PORT:-5432}";
                printf 'DB_DATABASE=%s\n' "$DB_DATABASE";
                printf 'DB_USERNAME=%s\n' "$DB_USERNAME";
                printf 'DB_PASSWORD=%s\n' "$DB_PASSWORD";
              } > /work/.env
          env:
            - name: APP_KEY
              value: ${SECRET_LINKACE_APP_KEY}
            - name: DB_CONNECTION
              value: pgsql
            - name: DB_HOST
              value: buvis-pg-cluster-rw.storage
            - name: DB_PORT
              value: "5432"
            - name: DB_DATABASE
              value: linkace
            - name: DB_USERNAME
              value: linkace
            - name: DB_PASSWORD
              value: ${SECRET_LINKACE_PG_PASSWORD}
          volumeMounts:
            - name: env-file
              mountPath: /work
        - name: artisan-clear
          image: ghcr.io/kovah/linkace:v2.3.1@sha256:8e8bb64d33a9f9581430e1fe46d6a920ecf40be4da33a13180ae094265b38146
          imagePullPolicy: IfNotPresent
          workingDir: /app
          command: ["sh", "-lc"]
          args:
            - >
              php /app/artisan config:clear &&
              php /app/artisan cache:clear &&
              php /app/artisan route:clear || true
          volumeMounts:
            - name: la-cache
              mountPath: /app/bootstrap/cache
      containers:
        - image: ghcr.io/kovah/linkace:v2.3.1@sha256:8e8bb64d33a9f9581430e1fe46d6a920ecf40be4da33a13180ae094265b38146
          name: linkace
          env:
            - name: APP_KEY
              value: ${SECRET_LINKACE_APP_KEY}
            - name: DB_CONNECTION
              value: pgsql
            - name: DB_HOST
              value: buvis-pg-cluster-rw.storage
            - name: DB_PORT
              value: "5432"
            - name: DB_DATABASE
              value: linkace
            - name: DB_USERNAME
              value: linkace
            - name: DB_PASSWORD
              value: ${SECRET_LINKACE_PG_PASSWORD}
          ports:
            - containerPort: 80
          volumeMounts:
            - name: la-cache
              mountPath: /app/bootstrap/cache
            - name: env-file
              mountPath: /app/.env
              subPath: .env
      restartPolicy: Always
