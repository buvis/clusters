---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: linkace
  namespace: gtd
  labels:
    app.kubernetes.io/name: linkace
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: linkace
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: linkace
    spec:
      volumes:
        - name: la-cache
          emptyDir: {}
        - name: env-file
          emptyDir: {}
      initContainers:
        - name: write-env-as-app
          image: linkace/linkace:latest@sha256:eeb8e290b9b85861839ec1ed156ee6b8df0a344a6b31ef856f0af1ed3c9a5995
          workingDir: /app
          command: ["sh", "-lc"]
          args:
            - >
              umask 027 &&
              mkdir -p /work &&
              {
                printf 'APP_KEY=%s\n' "$APP_KEY";
                printf 'APP_URL=%s\n' "$APP_URL";
                printf 'DB_CONNECTION=%s\n' "${DB_CONNECTION:-pgsql}";
                printf 'DB_HOST=%s\n' "$DB_HOST";
                printf 'DB_PORT=%s\n' "${DB_PORT:-5432}";
                printf 'DB_DATABASE=%s\n' "$DB_DATABASE";
                printf 'DB_USERNAME=%s\n' "$DB_USERNAME";
                printf 'DB_PASSWORD=%s\n' "$DB_PASSWORD";
              } > /work/.env
          env:
            - name: APP_KEY
              value: ${SECRET_LINKACE_APP_KEY}
            - name: APP_URL
              value: https://bookmarks.buvis.net
            - name: DB_CONNECTION
              value: pgsql
            - name: DB_HOST
              value: buvis-pg-cluster-rw.storage
            - name: DB_PORT
              value: "5432"
            - name: DB_DATABASE
              value: linkace
            - name: DB_USERNAME
              value: linkace
            - name: DB_PASSWORD
              value: ${SECRET_LINKACE_PG_PASSWORD}
          volumeMounts:
            - name: env-file
              mountPath: /work
        - name: artisan-clear
          image: ghcr.io/kovah/linkace:v2.5.7@sha256:227f6deac099e0c093824f0f1ad19616ed7e88d4a7b57e91c25de4bfb04246a8
          imagePullPolicy: IfNotPresent
          workingDir: /app
          command: ["sh", "-lc"]
          args:
            - >
              php /app/artisan config:clear &&
              php /app/artisan cache:clear &&
              php /app/artisan route:clear || true
          volumeMounts:
            - name: la-cache
              mountPath: /app/bootstrap/cache
      containers:
        - image: ghcr.io/kovah/linkace:v2.5.7@sha256:227f6deac099e0c093824f0f1ad19616ed7e88d4a7b57e91c25de4bfb04246a8
          name: linkace
          env:
            - name: APP_KEY
              value: ${SECRET_LINKACE_APP_KEY}
            - name: APP_URL
              value: https://bookmarks.buvis.net
            - name: DB_CONNECTION
              value: pgsql
            - name: DB_HOST
              value: buvis-pg-cluster-rw.storage
            - name: DB_PORT
              value: "5432"
            - name: DB_DATABASE
              value: linkace
            - name: DB_USERNAME
              value: linkace
            - name: DB_PASSWORD
              value: ${SECRET_LINKACE_PG_PASSWORD}
          ports:
            - containerPort: 80
          volumeMounts:
            - name: la-cache
              mountPath: /app/bootstrap/cache
            - name: env-file
              mountPath: /app/.env
              subPath: .env
      restartPolicy: Always
